[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bookmark-curator-agent"
version = "0.1.0"
description = "MVP local para curar y priorizar bookmarks"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
